import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rj.analista',
  appName: 'Analista de Cadastro R&J',
  webDir: 'dist'
};

export default config;
